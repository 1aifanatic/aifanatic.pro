import React, { useMemo, useState } from "react";
import ContainerBlock from "@components/ContainerBlock";
import PageIntro from "@components/PageIntro";
import userData from "@constants/data";

export default function SolopreneurProjects() {
  const [filter, setFilter] = useState("All");
  const categories = ["All", ...new Set((userData.solopreneurProjects || []).map((project) => project.category))];
  const projects = useMemo(() => (userData.solopreneurProjects || []).filter((project) => filter === "All" || project.category === filter), [filter]);
  return <ContainerBlock title="Independent products — Naveen Chatlapalli" description="Independent products and experiments by Naveen Chatlapalli."><PageIntro eyebrow="Independent products" title="A practice of shipping useful ideas.">An evolving collection of AI, productivity, and community tools built outside of client work.</PageIntro><section className="site-container page-section"><div className="flex flex-wrap gap-2">{categories.map((category) => <button key={category} type="button" onClick={() => setFilter(category)} className={`rounded-full border px-4 py-2 text-sm font-semibold transition ${filter === category ? "border-[#174b8b] bg-[#174b8b] text-white dark:border-[#a8c7ee] dark:bg-[#a8c7ee] dark:text-[#18211f]" : "border-[#d8ddd8] text-[#46514c] hover:border-[#174b8b] dark:border-[#46514c] dark:text-[#c5cec8]"}`}>{category}</button>)}</div><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{projects.map((project) => <a key={project.id} href={project.url.startsWith("http") ? project.url : `https://${project.url}`} target="_blank" rel="noopener noreferrer" className="paper-card group flex min-h-[240px] flex-col transition hover:-translate-y-1 hover:border-[#91aed2]"><div className="flex items-start justify-between gap-4"><p className="eyebrow">{project.category}</p><span className="text-[#174b8b] dark:text-[#a8c7ee]">↗</span></div><h2 className="mt-4 text-3xl">{project.name}</h2><p className="mt-3 leading-7 text-[#5f6864] dark:text-[#b7c0bb]">{project.description}</p><div className="mt-auto pt-6 text-xs font-semibold uppercase tracking-[.13em] text-[#5f6864] dark:text-[#b7c0bb]">{project.stage}</div></a>)}</div></section></ContainerBlock>;
}
